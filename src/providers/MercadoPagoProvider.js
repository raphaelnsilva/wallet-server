// lição de casa, implementar um validação de segurança com typescript chamada IpaymentProvider

class MercadoPagoProvider {
  async process({
    transactionCode,
    total,
    installments,
    creditCard,
    customer,
    billing,
    items,
  }) {

  }
}

export default MercadoPagoProvider;